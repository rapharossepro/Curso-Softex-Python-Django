Explicação do código e suas ações em ordem de execução
------------------------------------------------------

1. Inicialização de variáveis
   - registros_acessos = [] → lista que guarda todos os registros de acesso em forma de tupla (usuário, status, duração).
   - usuarios_sucesso = set() → conjunto que armazena apenas os nomes dos usuários que tiveram pelo menos um acesso bem-sucedido.
   - tempo_total_sucesso = 0 → variável acumuladora do tempo total de sessões com status "Sucesso".

2. Estrutura de repetição principal (while True)
   - O programa fica em loop até que o usuário digite "0" para encerrar.

3. Entrada do nome do usuário
   - Solicita o nome de usuário.
   - Se for "0" → encerra o programa.
   - Se for vazio → mostra aviso e não registra.

4. Escolha do status de acesso
   - Exibe um pequeno menu: "1 - Sucesso" ou "2 - Falha".
   - Enquanto o usuário não digitar 1 ou 2, o programa repete a pergunta.
   - Ao digitar corretamente, atribui a string correspondente: "Sucesso" ou "Falha".

5. Entrada da duração da sessão
   - Solicita um número inteiro (em minutos).
   - Caso o valor digitado não seja número, mostra aviso e descarta.
   - Caso o valor seja negativo, mostra aviso e descarta.
   - Se for válido, segue o fluxo.

6. Registro do acesso
   - Cria uma tupla (usuario, status, duracao).
   - Adiciona essa tupla à lista registros_acessos.

7. Atualização de dados em caso de sucesso
   - Se o status for "Sucesso":
       • Adiciona o usuário ao conjunto usuarios_sucesso.
       • Soma a duração ao tempo_total_sucesso.

8. Confirmação
   - Exibe mensagem: "Registro efetuado com sucesso."

9. Fim do loop
   - Retorna ao passo 3 até que seja digitado "0".

10. Exibição dos resultados finais
   - Mostra todos os registros armazenados (lista de tuplas).
   - Mostra a lista de usuários que tiveram pelo menos um acesso com sucesso.
   - Mostra o tempo total acumulado das sessões bem-sucedidas.

------------------------------------------------------
Resumo:
- O programa coleta dados de acessos (usuário, status, duração).
- Armazena cada acesso como tupla dentro de uma lista.
- Mantém controle de usuários com sucesso em um conjunto (sem repetições).
- Calcula o tempo total de acessos bem-sucedidos.
- Finaliza mostrando um resumo geral.
"""

with open("/mnt/data/explicacao_codigo.txt", "w", encoding="utf-8") as f:
    f.write(explicacao)

"/mnt/data/explicacao_codigo.txt"
